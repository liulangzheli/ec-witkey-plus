<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liulangzheli.ecwitkeyplus.ecwitkey.mapper.ProjectOrderMapper">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_id, create_time, pay_time, examine_time, pick_time, check_time, use_type, category_id, major, province, city, end_time, period, soft_supplier, soft_name, intro, state, remark
    </sql>

    <!-- 通用查询结果列 -->
    <sql id="User_And_Base_Column_List">
        id, user_id, create_time, pay_time, examine_time, pick_time, check_time, use_type, category_id, major, province, city, end_time, period, soft_supplier, soft_name, intro, state, remark, nickname, user_type, company_name, licence_id, id_num
    </sql>

    <select id="getProjectOrderById" resultType="com.liulangzheli.ecwitkeyplus.ecwitkey.vo.ProjectOrderQueryVo">
        select
        <include refid="Base_Column_List"/>
        from project_order where id = #{id}
    </select>

    <select id="getProjectOrderByUserId" resultType="com.liulangzheli.ecwitkeyplus.ecwitkey.vo.ProjectOrderUserQueryVo">
        select
        <include refid="Base_Column_List"/>
        from project_order where user_id = #{userId}
    </select>

    <select id="getProjectOrderUserById" resultType="com.liulangzheli.ecwitkeyplus.ecwitkey.vo.ProjectOrderQueryVo">
        select
        <include refid="User_And_Base_Column_List"/>
        from project_order m,sys_user s where m.user_id=s.id and id = #{id}
    </select>

    <select id="getProjectOrderPageList" resultType="com.liulangzheli.ecwitkeyplus.ecwitkey.vo.ProjectOrderQueryVo">
        select
        <include refid="Base_Column_List"/>
        from project_order
        <where><!-- 某些为空时用来组装语句 -->
            1 = 1
            <if test="param.state != null">
                AND state=#{param.state}
            </if>
            ORDER BY create_time asc
        </where>
    </select>

</mapper>
